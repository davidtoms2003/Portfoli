// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	// Set your production site and base path for GitHub Pages project sites
	site: 'https://u1979286.github.io/portfoli/',
	base: '/portfoli/',
	devToolbar: {
		enabled: false,
	},
});
