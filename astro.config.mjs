// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	// GitHub Pages project configuration
	site: 'https://u1979286.github.io/portfoli/',
	base: '/portfoli/',
	devToolbar: {
		enabled: false,
	},
});
