---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="About" description="About the filmmaker behind Film Diary.">
	<section class="about">
		<header class="about__header">
			<p class="eyebrow">About me</p>
			<h1>David Tomás Velasco</h1>
			<p>
				Filmmaker and cinematographer searching for shared memories. My camera travels with me from the Caucasus mountains to the temples of Asia. I chase natural light, ambient sound, and the delicacy of spontaneous portraits.
			</p>
		</header>
		<div class="about__grid">
			<div class="about__card">
				<h2>Process</h2>
				<p>
					I work with lightweight crews to capture intimate narratives. I’m interested in filming the energy of a group of friends, the silence of a community, and landscapes that invite us to breathe slower.
				</p>
			</div>
			<div class="about__card">
				<h2>Influences</h2>
				<p>
					I’m drawn to independent cinema, ambient music, and travel journals. Every video is edited as if it were a letter to the people who shared the road with me.
				</p>
			</div>
			<div class="about__card">
				<h2>Collaborations</h2>
				<p>
					I’m open to collaborating with studios, brands, or artists looking for honest visual narratives grounded in the human experience.
				</p>
			</div>
		</div>
	</section>

	<style>
		.about {
			display: grid;
			gap: clamp(2.5rem, 6vw, 4rem);
		}

		.about__header {
			display: grid;
			gap: 1.4rem;
			max-width: 40rem;
		}

		.about__header h1 {
			margin: 0;
			font-family: var(--font-display);
			font-size: clamp(2.8rem, 6vw, 4.6rem);
			letter-spacing: 0.28em;
			text-transform: uppercase;
		}

		.about__header p {
			margin: 0;
			font-size: clamp(1rem, 1.7vw, 1.25rem);
			line-height: 1.9;
			color: rgba(255, 255, 255, 0.78);
		}

		.about__grid {
			display: grid;
			grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
			gap: clamp(1.8rem, 4vw, 2.6rem);
		}

		.about__card {
			padding: clamp(1.8rem, 4vw, 2.6rem);
			border-radius: clamp(18px, 3vw, 24px);
			background: linear-gradient(140deg, rgba(255, 255, 255, 0.08), rgba(255, 255, 255, 0));
			border: 1px solid rgba(255, 255, 255, 0.08);
			box-shadow: 0 24px 60px rgba(0, 0, 0, 0.32);
			display: grid;
			gap: 0.9rem;
		}

		.about__card h2 {
			margin: 0;
			font-size: clamp(1.1rem, 2vw, 1.6rem);
			letter-spacing: 0.2em;
			text-transform: uppercase;
		}

		.about__card p {
			margin: 0;
			line-height: 1.8;
			color: rgba(255, 255, 255, 0.78);
		}
	</style>
</BaseLayout>
